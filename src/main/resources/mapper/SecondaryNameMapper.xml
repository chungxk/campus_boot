<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chung.campus.mapper.SecondaryNameMapper">

  <select id="getAll" resultType="com.chung.campus.entity.SecondaryName">
    select * from secondary_college
    where sid != 0;
  </select>

</mapper>